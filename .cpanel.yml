---
deployment:
  tasks:
    - export DEPLOY_PATH="/home/ylrdxapi/domains/peerview2"
    - /bin/cp -r dist/ $DEPLOY_PATH
    - /bin/cp -r views/ $DEPLOY_PATH
    - /bin/cp package.json package-lock.json $DEPLOY_PATH
